cl-url is a small library that lets you treat URL as first class CL objects (a lot like what cl-fad does for pathnames). See test.lisp for what you can do with it.

Note the the library is in alpha stage and still buggy, although usable for general cases.